/** @type {import('next').NextConfig} */
const nextConfig = {
  // Si querés Cloudflare Pages estático: descomentá
  // output: "export",
  // images: { unoptimized: true },
};

export default nextConfig;
